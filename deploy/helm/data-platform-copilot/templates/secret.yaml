{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "data-platform-copilot.fullname" . }}-secrets
type: Opaque
stringData:
  llm-api-key: {{ .Values.secrets.llmApiKey | default "" | quote }}
{{- end }}
