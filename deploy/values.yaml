replicaCount: 2

image:
  repository: your-docker-repo/data-platform-copilot
  tag: v0.10.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8001

resources:
  limits:
    cpu: "1"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"
